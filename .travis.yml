language: java
jdk:
  - oraclejdk8
script: "mvn cobertura:cobertura"
cache:
  directories:
  - $HOME/.m2
after_success:
  - bash <(curl -s https://codecov.io/bash)
  - mvn clean test
services:
  - redis-server